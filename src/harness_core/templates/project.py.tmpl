#!/usr/bin/env python3
"""Project-specific remote tracker operations.

Generated placeholder. Keep repository-specific Forgejo/Jira/GitHub operations
here when a project needs a richer wrapper than the global skills provide.
"""

from __future__ import annotations

import sys


def main() -> int:
    print(
        "project.py is reserved for project-specific tracker operations; "
        "use .claude/scripts/harness_cli.py for local harness commands.",
        file=sys.stderr,
    )
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
